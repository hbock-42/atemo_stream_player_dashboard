<?xml version="1.0" encoding="utf-8"?>
<!--
  Album artwork from this device is often served from the device itself over
  plain HTTP (http://<deviceIp>:8008/...), which Android 9+ blocks by default.

  Cleartext is permitted only for private address ranges, never globally: the
  CASTV2 connection itself is TLS and needs no exception, and opening cleartext
  app-wide to solve an artwork problem would be a poor trade.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.0.0/8</domain>
        <domain includeSubdomains="false">172.16.0.0/12</domain>
        <domain includeSubdomains="false">192.168.0.0/16</domain>
        <domain includeSubdomains="false">169.254.0.0/16</domain>
        <domain includeSubdomains="true">local</domain>
    </domain-config>
</network-security-config>
